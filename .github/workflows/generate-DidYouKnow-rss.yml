name: Generate DidYouKnow RSS Feed

on:
  schedule:
    # This cron schedule runs at minute 20 past every 6th hour.
    - cron: '20 */6 * * *'
  workflow_dispatch: # This allows you to trigger the workflow manually for testing

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required so the bot can push the new rss.xml file to your repo

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Run generation script
        run: node generate-rss-DidYouKnow.js

      - name: Commit and Push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # CHANGE THIS LINE: Add history.json to the list
          git add DidYouKnow.xml history-DidYouKnow.json cycle-state-DidYouKnow.json
          git commit -m "chore: update RSS feed and quote history" || exit 0

          git push





